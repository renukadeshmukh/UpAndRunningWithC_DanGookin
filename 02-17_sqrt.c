#include <stdio.h>
#include <math.h>

int main()
{
    float r;

    r = sqrt(2.0);

	printf("The square root of 2.0 is %f\n",r);
    printf("The square root of 2 is %f\n",sqrt(2));

    printf("The square root of 4 is %f\n",sqrt(4));
    printf("The square root of 4.0 is %f\n",sqrt(4.0));

	return(0);
}
